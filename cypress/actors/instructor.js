class Instructor {
    constructor(first_name, last_name, email, title, bio) {
        this.first_name = first_name
        this.last_name = last_name
        this.email = email
        this.title = title
        this.bio = bio
    }
}

export default Instructor;